# Git Workflow (Apps)

- Protect `main`; merge through pull requests.
- Branch name prefixes: `feat/`, `fix/`, `chore/`, `hotfix/`.
- Keep migrations forward-only and reversible where practical.
- Deployable state lives on `main`; cut releases via tags per the app's deploy
  process.
